# 项目协作规则

## 1. 项目目标与当前阶段

- 基于当前黑马点评项目的业务场景，重构一个适合 Java 后端本科生深入学习、复现，并用于实习/校招展示的校园及周边活动预约平台。
- 优先级是业务成立、数据流清楚、技术使用有原因、可测试、能讲清设计取舍。
- 不以维护旧系统、逐级迁移旧依赖或覆盖全部课程知识点为目标。
- 已确认方案见 [docs/PROJECT_PLAN.md](docs/PROJECT_PLAN.md)。本文件约束工作范围，方案文档定义业务与数据边界。
- Stage 1 已完成，用户已掌握并手动提交 GitHub。用户于 2026-09-12 授权实施 Stage 2：Activity；当前实施与验证状态见方案文档第 10 节。Stage 3～5 仍等待后续指令。
- 用户后续明确指令优先于本文件；范围变化时同步维护这两份文档，避免同时保留互相矛盾的方案。

## 2. 第一版固定范围

正式业务模块仅有：

| 模块 | 职责 |
| --- | --- |
| account | 验证码登录、Redis token、续期、退出、获取当前用户、基本资料；固定 STUDENT / ORGANIZER 角色 |
| activity | 地点、活动、场次、发布、报名窗口、容量、分页、详情缓存、Redis GEO |
| registration | requestId、Lua 原子受理、RabbitMQ 异步报名、结果查询、消费幂等 |
| participation | 报名凭证核验、权限与重复核验保护、参与记录、到场名单 |

- 保持单 Maven 模块、单 Spring Boot 应用；按业务组织代码，使用 Controller → Service → Mapper/外部组件的清晰调用关系。
- Listener 接收消息并调用业务 Service，不在监听器中堆积报名事务规则。
- 使用已约定的 Java 21、Spring Boot 3.x、MyBatis-Plus、MySQL、Redis、RabbitMQ、Docker Compose 及必要测试工具。具体兼容补丁版本在实施时锁定。
- Stage 2 使用 Redisson 缓存重建锁处理热点缓存击穿；不能为了展示锁而重复保护 Lua 已经完成的同一原子动作。
- 时间戳 + Redis INCR 仅作为小型 ID 生成工具，不建设独立 ID 服务。
- 不增加关注、Feed、点赞、社区、Bitmap 连续签到/跨月统计、UV 等第一版业务。
- 不增加支付、退款、候补、选座、复杂排期或取消后的自动名额返还。

## 3. 报名链路不可拆分的约束

1. 接口校验登录与基本参数，生成候选 requestId。
2. 一次 Redis Lua 执行完成独立报名配置校验、防重复、剩余名额检查、名额预占、用户预占记录和 requestId 的 PENDING 写入。
3. Lua 成功之后才发送 RabbitMQ；发送 MQ 前不写 MySQL 报名请求或结果记录。
4. 消费者在 MySQL 中保存正式报名、容量变化和 requestId 的最终结果。
5. 事务提交后才 ACK。通过 requestId 唯一约束及用户与场次的业务唯一约束保证消费幂等。
6. 不恢复之前讨论过的“先落库待处理请求，再执行 Lua/发送 MQ”方案。

## 4. 必须保持的四个设计边界

### Redis 暂态生命周期

- PENDING、用户占位和场次运行配置采用共同的绝对过期边界：场次结束时间 + 缓冲期；初始缓冲期为 24 小时，可配置。
- 不采用几秒或几分钟的预占 TTL，不做自动超时释放，不因为查询或重复请求而延长生命周期。
- 场次结束后，未完成的旧消息不得再创建成功报名；已在执行的消费事务必须有界，缓冲期应覆盖最长允许处理时间和合理时钟偏差。
- 迟到消息先查询 MySQL 既有终态，不得将 SUCCESS 改成 FAILED。没有终态且超过业务截止时间时，只能记录明确失败。
- Redis 到期表示暂态清理，不表示报名失败、名额归还或数据库记录可以删除。

### Lua 配置来源

- 活动详情展示缓存与报名运行状态分离。Lua 不使用可能陈旧的展示缓存。
- MySQL 保存场次配置事实；场次发布提交后，将完整配置及初始可预占名额原子初始化到独立 Redis 运行状态。
- 未初始化或同步失败的场次不得受理报名；禁止根据缺失 key 临时猜测配置或自动重建活动中的库存。
- 第一版核心报名配置仅草稿可修改，发布后冻结容量、报名窗口和场次时间。发布后可改展示内容，修改展示内容只使展示缓存失效。
- 重复发布/同步不得覆盖已有库存、用户占位或 PENDING。具体发布失败与重试规则见方案文档。

### requestId 终态

- Redis 保存 PENDING 暂态；MySQL 保存 SUCCESS/FAILED 终态，不在 MySQL 中增加发送前的 PENDING 记录。
- 结果查询先查 MySQL 终态，再查 Redis PENDING，并验证用户归属。
- MySQL 与 Redis 都查不到时返回“未查询到结果”，不伪造 FAILED，不声称名额已释放。
- 技术异常、确认超时和死信本身不等于确定的业务失败，不得直接生成 FAILED 终态。

### MQ 发送异常的接口语义

- 一旦确认 Lua 已成功受理，无论发送 MQ 正常、抛异常或确认尚不明确，都保留原 requestId，返回 HTTP 202 和 PENDING 的受理语义。
- 说明“尚未得到最终结果”，不能说明“报名失败，请重新提交”或“报名已经成功”。
- 异步确认失败记录关联 requestId 的日志；不通过客户端重试间接构造自动恢复机制。
- 重复提交发现原预占时返回原 requestId，不重新扣名额，不创建新的 PENDING，不自动补发消息。

## 5. 可靠性范围

- 保留 Publisher Confirm、Consumer ACK、requestId、消费幂等、数据库事务和唯一约束、持久化消息与队列、基础死信队列、发送与消费日志。
- 不实施 Outbox、Saga、分布式事务框架、通用补偿/重试平台、自动库存对账、自动重放或复杂故障恢复系统。
- Lua 成功但 MQ 发送前进程宕机等故障窗口写入文档；允许第一版出现未自动恢复的 PENDING，不能声称端到端绝不丢失或恰好一次。
- reconciliation 仅作为未来可能的核对修复方向，不列为第一版待实现任务或验收门槛。

## 6. 固定的五个 Stage

按 account → activity → registration → participation 完成业务，最后集中整理工程验证与展示材料。

| 阶段 | 交付范围 | 学习重点 |
| --- | --- | --- |
| Stage 1：项目基础 + Account | Java 21 / Spring Boot 3.x 应用；Docker Compose 启动 MySQL、Redis、RabbitMQ；验证码、登录、Token 续期、退出、当前用户与固定角色 | 项目结构、配置管理、Docker 基础服务、Redis Token、Interceptor、ThreadLocal / UserContext、登录请求生命周期 |
| Stage 2：Activity | 组织者创建活动、场次并发布；学生查询列表、详情、附近活动；独立报名运行配置初始化 | MySQL、Cache Aside、缓存空值、随机 TTL、缓存击穿、Redisson 重建锁、更新后缓存失效、Redis GEO |
| Stage 3：Registration | requestId → 一次 Lua 原子受理 → RabbitMQ → 消费者 MySQL 事务 → 终态查询 | Lua 原子性、并发、Redis INCR ID、Publisher Confirm、Consumer ACK、削峰、事务、唯一约束、消费幂等、异步状态 |
| Stage 4：Participation | 成功报名凭证、组织者核验、参与记录与到场名单，完成业务闭环 | 权限归属、核验时间、数据完整性、MySQL 约束、事务、重复核验幂等 |
| Stage 5：工程化 + 简历化 | 补齐并整理测试、真实压测、日志、Docker 运行交付、README、架构图及可供用户发布的展示材料 | 可复现环境、容量与幂等验证、性能测量、结果解释、真实可核验的项目证据 |

- Stage 1 保持轻量：RabbitMQ 只完成环境与连通性验证，报名消息拓扑和消费链路留到 Stage 3；不提前搭建通用基础设施平台。
- Stage 2、Stage 3 和 Stage 5 是主要投入点，分别深入缓存、Lua + MQ + MySQL 的一致性边界与幂等、真实压测和项目证据。
- 必要测试随功能一起完成，每阶段都能编译、启动和通过相关测试；Stage 5 集中补齐与整理，不把前四阶段的正确性验证全部推迟到最后。
- Stage 5 不增加业务模块或技术栈。压测按环境能力逐档尝试 500 / 1000 / 2000 并发，数值是测试计划，不是已实现的性能承诺。
- 详细目标、实现顺序与验收标准见方案文档第 9 节。Stage 1、Stage 2 已获实施授权，实际交付与验证状态见方案文档第 10 节；Stage 3～5 仍等待后续指令。

## 7. 实施、学习与验证

- Git 由用户手动管理。除非用户后续明确要求，Agent 不执行 Git 命令，也不代替用户发布 GitHub；文档和代码编辑均须保留用户已有修改。
- 实施时先解释目标、涉及模块和文件、数据流变化及验证方法；选择完成当前目标的最小实现。
- 每次围绕一个业务层级或一条链路讲解。解释为什么这样设计，再解释关键代码。
- 不批量生成无关文档，不一次引入大量抽象层或全部课程演示版本。
- 每阶段验收包含编译、启动和相关测试。使用隔离 MySQL、Redis、RabbitMQ 数据，不直接运行会污染现有环境的压测或预热工具。
- 文档修改只验证文档与差异，不启动会写数据的应用或测试。
- 压测同时检查受理吞吐量、最终落库结果、处理延迟、错误及容量约束；只报告真实测得的数据和测试条件。
- 阶段交付说明修改目的、模块职责、关键决策、复现顺序和可观察的验收结果。
